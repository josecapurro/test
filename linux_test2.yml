- name: Test Windows
  hosts: windows
  gather_facts: no

  tasks:
    - name: linux ping
      ansible.windows.win_ping:

    - name: Ejecutar ipconfig
      ansible.windows.win_command:
        cmd: ipconfig
